{% if HasDescription -%}
--- <summary>{{ Description | csharpdocs }}</summary>
{% endif -%}
{% for enum in Enums -%}
{%   if IsStringEnum -%}
--    [System.Runtime.Serialization.EnumMember(Value = @"{{ enum.Value | replace: '"', '""' }}")]
{%   endif -%}
{% if IsEnumAsBitFlags -%}
--    {{ enum.Name }} = {{ enum.InternalFlagValue }},
{% else -%}
--    {{ enum.Name }} = {{ enum.InternalValue }},
{% endif -%}

{% endfor -%}
-- type {{Name}} is pls_integer;
